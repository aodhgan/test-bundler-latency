"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRepresentation = void 0;
const formatString_js_1 = require("./formatString.js");
function getRepresentation(value, type, options) {
    if (type === 'Boolean') {
        return value.toString();
    }
    else if (type === 'Number') {
        return value.toString();
    }
    else if (type === 'String') {
        return (0, formatString_js_1.formatString)(value.toString(), options);
    }
    else if (type === 'RegExp') {
        return value.toString();
    }
    else if (type === 'Date') {
        const iso = value.toISOString();
        if (iso.endsWith('T00:00:00.000Z')) {
            return iso.slice(0, -14);
        }
        else if (iso.endsWith('.000Z')) {
            return iso.slice(0, -5) + 'Z';
        }
        else {
            return iso;
        }
    }
}
exports.getRepresentation = getRepresentation;
//# sourceMappingURL=getRepresentation.js.map
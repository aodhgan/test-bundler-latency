import { format } from './format.js';
const FORMAT_OPTIONS = {
    compareErrorStack: false,
    ignorePrototypes: false,
    minusZero: false,
    uniqueNaNs: false,
    indentSize: 0,
    inline: true,
    maxLineLength: 30,
    skipMatcherReplacement: true,
    requireStrictEquality: false,
};
export function formatCompact(value, maxLineLength) {
    return format(value, null, {
        ...FORMAT_OPTIONS,
        maxLineLength: maxLineLength !== null && maxLineLength !== void 0 ? maxLineLength : FORMAT_OPTIONS.maxLineLength,
    });
}
//# sourceMappingURL=formatCompact.js.map
export function isEqualNumber(value, other, options) {
    if (isNaN(value)) {
        return options.uniqueNaNs ? false : isNaN(other);
    }
    else if (value === 0) {
        return options.minusZero ? Object.is(value, other) : other === 0;
    }
    else {
        return value === other;
    }
}
//# sourceMappingURL=isEqualNumber.js.map
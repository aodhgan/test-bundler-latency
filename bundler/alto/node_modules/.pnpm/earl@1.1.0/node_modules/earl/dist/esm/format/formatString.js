export function formatString(value, options) {
    if (options.inline && value.length > options.maxLineLength - 2) {
        return JSON.stringify(value.slice(0, 7) + '...');
    }
    return JSON.stringify(value);
}
//# sourceMappingURL=formatString.js.map
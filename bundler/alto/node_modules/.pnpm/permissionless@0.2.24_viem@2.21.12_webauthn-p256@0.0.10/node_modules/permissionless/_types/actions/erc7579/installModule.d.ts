import type { Address, Client, Hex, Narrow, OneOf } from "viem";
import { type GetSmartAccountParameter, type PaymasterActions, type SmartAccount, type UserOperationCalls } from "viem/account-abstraction";
import type { ModuleType } from "./supportsModule.js";
export type InstallModuleParameters<TSmartAccount extends SmartAccount | undefined, calls extends readonly unknown[]> = GetSmartAccountParameter<TSmartAccount> & {
    type: ModuleType;
    address: Address;
    maxFeePerGas?: bigint;
    maxPriorityFeePerGas?: bigint;
    nonce?: bigint;
    calls?: UserOperationCalls<Narrow<calls>>;
    paymaster?: Address | true | {
        /** Retrieves paymaster-related User Operation properties to be used for sending the User Operation. */
        getPaymasterData?: PaymasterActions["getPaymasterData"] | undefined;
        /** Retrieves paymaster-related User Operation properties to be used for gas estimation. */
        getPaymasterStubData?: PaymasterActions["getPaymasterStubData"] | undefined;
    } | undefined;
    /** Paymaster context to pass to `getPaymasterData` and `getPaymasterStubData` calls. */
    paymasterContext?: unknown | undefined;
} & OneOf<{
    context: Hex;
} | {
    initData: Hex;
}>;
export declare function installModule<TSmartAccount extends SmartAccount | undefined, calls extends readonly unknown[]>(client: Client, parameters: InstallModuleParameters<TSmartAccount, calls>): Promise<Hex>;
//# sourceMappingURL=installModule.d.ts.map